return {
	CLIENT_DETECTION_MULTI = Vector3.new(1.2, 1, 1.2);
	SERVER_VALIDATION_MULTI = Vector3.new(1.9, 1, 1.9); -- Multiplies the size of the client zone by each of these values, so we have a bigger validation zone on server
	ZONE_TAG = 'Privacy_Zone';
}